# Checkpoint Skill

체크포인트 시스템 워크플로우입니다. "체크포인트 저장", "작업 저장", "복원" 등의 요청 시 사용하세요.

---

## 📋 체크포인트란?

작업 중 특정 시점의 상태를 저장하여 나중에 복원할 수 있게 하는 시스템입니다.

- **Git 프로젝트**: 자동 커밋으로 저장
- **비Git 프로젝트**: MD 파일로 상태 기록

---

## 📋 기본 명령어

### 저장

```
"체크포인트 저장해줘"
"checkpoint save"
"작업 상태 저장"
"현재 상태 백업"
```

### 복원

```
"체크포인트 복원해줘"
"checkpoint restore"
"이전 상태로 돌아가줘"
"마지막 저장점으로"
```

### 목록 확인

```
"체크포인트 목록 보여줘"
"저장된 체크포인트 확인"
```

---

## 📋 Git 프로젝트에서의 동작

### 저장 시

```bash
# 1. 변경사항 스테이징
git add .

# 2. 체크포인트 커밋
git commit -m "checkpoint: [설명]"

# 커밋 메시지 형식
checkpoint: 작업 설명
checkpoint: API 엔드포인트 구현 중
checkpoint: 버그 수정 진행 중
```

### 복원 시

```bash
# 최근 체크포인트로 복원
git checkout HEAD~1

# 특정 체크포인트로 복원
git checkout [commit-hash]
```

---

## 📋 비Git 프로젝트에서의 동작

### 저장 시

`!AGENT_System/` 폴더에 MD 파일 생성:

```markdown
# Checkpoint: 2026-01-29 08:53:00

## 작업 설명
API 엔드포인트 구현 중

## 변경된 파일
- src/api/users.ts (수정)
- src/types/user.ts (추가)

## 현재 상태
- 사용자 조회 API 완료
- 사용자 생성 API 진행 중

## 메모
- 다음 작업: 유효성 검증 추가
```

### 복원 시

1. 체크포인트 MD 파일 읽기
2. 변경된 파일 목록 확인
3. 해당 시점의 상태로 수동 복원 안내

---

## 📋 권장 사용 시점

| 시점 | 이유 |
|------|------|
| 대규모 리팩토링 전 | 안전하게 실험 가능 |
| 작업 중단 시 | 상태 보존 |
| 실험적 변경 전 | 롤백 가능 |
| 중요 기능 완료 후 | 마일스톤 기록 |

---

## 📋 체크포인트 vs Git 커밋

| 구분 | 체크포인트 | 일반 커밋 |
|------|-----------|----------|
| 목적 | 임시 저장, 복원점 | 영구 기록 |
| 메시지 | 간단한 설명 | 상세한 변경 내용 |
| 빈도 | 자주 (30분~1시간) | 기능 단위 |
| 푸시 | 선택적 | 필수 |

---

## 📋 커맨드 라이브러리

상세 커맨드는 `~/.cursor/_COMMAND_LIBRARY/checkpoint-commands.md` 참조:

```
@command: checkpoint_save
@keywords: 체크포인트,저장,백업,save,checkpoint
@triggers: "체크포인트 저장", "작업 저장", "현재 상태 저장"

@command: checkpoint_restore
@keywords: 체크포인트,복원,롤백,restore,checkpoint
@triggers: "체크포인트 복원", "이전 상태로", "롤백"
```

---

## 🚫 주의사항

1. **민감 정보 포함 주의** - .env 파일 등 제외
2. **대용량 파일 주의** - node_modules 등 제외
3. **복원 전 현재 상태 확인** - 덮어쓰기 방지
4. **정기적 저장 권장** - 30분~1시간 간격
