---
description: "Git 워크플로우 스킬 - 브랜치 전략 및 협업 가이드"
alwaysApply: false
---

# Git Workflow Skill

Git 브랜치 전략 및 협업 워크플로우 가이드입니다.

---

## 🌳 브랜치 전략

### 1. GitHub Flow (권장 - 소규모/CI-CD)

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                      GitHub Flow                            │
│                                                             │
│  main ──●────●────●────●────●────●────●────●──              │
│          ╲        ↗    ╲        ↗    ╲      ↗               │
│           ╲──●──●╱      ╲──●──●╱      ╲──●─╱                │
│           feature-1    feature-2    feature-3               │
│                                                             │
│  규칙:                                                       │
│  - main은 항상 배포 가능                                     │
│  - feature 브랜치에서 작업                                   │
│  - PR로 main에 머지                                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2. Git Flow (복잡한 릴리즈 관리)

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                        Git Flow                             │
│                                                             │
│  main     ●──────────────────●────────────────●──           │
│            ╲                ↗                ↗              │
│  release   ╲────●────●────╱                ╱                │
│             ╲  ↗                          ╱                 │
│  develop ────●──────●──────●──────●──────●────              │
│               ╲    ↗╲    ↗                                  │
│  feature       ●──●  ●──●                                   │
│                                                             │
│  브랜치:                                                     │
│  - main: 프로덕션                                            │
│  - develop: 개발 통합                                        │
│  - feature/*: 기능 개발                                      │
│  - release/*: 릴리즈 준비                                    │
│  - hotfix/*: 긴급 수정                                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 📋 브랜치 네이밍

### 컨벤션

```bash
# 기능 개발
feature/user-authentication
feature/payment-integration
feature/JIRA-123-add-login

# 버그 수정
fix/login-redirect-error
fix/JIRA-456-memory-leak
bugfix/cart-total-calculation

# 긴급 수정
hotfix/security-vulnerability
hotfix/production-crash

# 리팩토링
refactor/database-layer
refactor/api-structure

# 문서
docs/api-documentation
docs/readme-update

# 실험/프로토타입
experiment/new-ui-framework
spike/performance-testing
```

### 명명 규칙

```
✅ 좋은 예
feature/add-user-profile
fix/cart-item-count
refactor/authentication-service

❌ 나쁜 예
feature/myFeature     # 불명확
fix                   # 설명 없음
john-branch           # 개인 이름
test123               # 의미 없음
```

---

## 📋 커밋 메시지

### Conventional Commits

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Type 종류

```
feat:     새 기능 추가
fix:      버그 수정
docs:     문서 변경
style:    코드 포맷팅 (기능 변경 없음)
refactor: 리팩토링 (기능 변경 없음)
perf:     성능 개선
test:     테스트 추가/수정
build:    빌드 시스템 변경
ci:       CI 설정 변경
chore:    기타 변경
revert:   커밋 되돌리기
```

### 예시

```bash
# 기능 추가
feat(auth): 소셜 로그인 추가

Google, GitHub OAuth 연동
- OAuth2 클라이언트 설정
- 콜백 핸들러 구현
- 사용자 프로필 연동

Closes #123

# 버그 수정
fix(cart): 수량 0 이하로 설정되는 버그 수정

최소 수량을 1로 제한하는 검증 추가

Fixes #456

# 리팩토링
refactor(api): 에러 핸들링 통일

- 커스텀 에러 클래스 도입
- 전역 에러 핸들러 구현
- 일관된 에러 응답 형식 적용

# Breaking Change
feat(api)!: API 응답 형식 변경

BREAKING CHANGE: 응답 wrapper가 { data, meta }에서 
{ data, pagination }으로 변경됨
```

### 커밋 메시지 규칙

```
✅ 좋은 커밋 메시지
- 명령형 현재 시제 사용 ("Add" not "Added")
- 첫 글자 소문자 (영문) 또는 명확한 설명 (한글)
- 72자 이내 제목
- 본문에 "왜" 설명

❌ 나쁜 커밋 메시지
- fix
- 수정
- WIP
- asdfasdf
- 여러 변경사항을 하나의 커밋에
```

---

## 📋 Pull Request 가이드

### PR 템플릿

```markdown
## 📋 변경 사항
<!-- 무엇을 변경했는지 -->
- 사용자 프로필 편집 기능 추가
- 이미지 업로드 구현

## 🎯 관련 이슈
<!-- 관련 이슈 번호 -->
Closes #123

## 🧪 테스트 방법
<!-- 어떻게 테스트할 수 있는지 -->
1. 프로필 페이지 접속
2. 편집 버튼 클릭
3. 정보 수정 후 저장

## 📸 스크린샷
<!-- UI 변경 시 첨부 -->

## ✅ 체크리스트
- [ ] 테스트 통과
- [ ] 린트 통과
- [ ] 문서 업데이트 (필요시)
- [ ] Breaking change 없음
```

### PR 크기 가이드

```
✅ 이상적인 PR
- 200-400 줄 변경
- 하나의 기능/수정
- 빠른 리뷰 가능

⚠️ 큰 PR은 분할
- 리팩토링 PR + 기능 PR
- 의존성 업데이트 별도 PR
- UI/로직 분리
```

---

## 📋 Git 명령어 치트시트

### 브랜치 작업

```bash
# 브랜치 생성 및 이동
git checkout -b feature/new-feature
git switch -c feature/new-feature  # 새 명령어

# 브랜치 목록
git branch          # 로컬
git branch -r       # 원격
git branch -a       # 전체

# 브랜치 삭제
git branch -d feature/done        # 머지된 브랜치
git branch -D feature/abandoned   # 강제 삭제

# 원격 브랜치 삭제
git push origin --delete feature/old
```

### 커밋 관리

```bash
# 마지막 커밋 수정 (push 전에만!)
git commit --amend -m "수정된 메시지"

# 커밋 취소 (변경사항 유지)
git reset --soft HEAD~1

# 커밋 취소 (변경사항 삭제)
git reset --hard HEAD~1

# 특정 커밋 되돌리기 (새 커밋 생성)
git revert <commit-hash>
```

### Stash

```bash
# 변경사항 임시 저장
git stash
git stash push -m "설명"

# 목록 확인
git stash list

# 적용
git stash pop         # 적용 후 삭제
git stash apply       # 적용 (삭제 안 함)
git stash apply stash@{1}  # 특정 stash

# 삭제
git stash drop stash@{0}
git stash clear       # 전체 삭제
```

### Rebase

```bash
# main 최신 변경사항 가져오기
git checkout feature/my-feature
git rebase main

# 인터랙티브 rebase (커밋 정리)
git rebase -i HEAD~3

# 충돌 해결 후
git rebase --continue

# rebase 취소
git rebase --abort
```

### 유용한 명령어

```bash
# 변경 이력 보기
git log --oneline --graph --all

# 특정 파일 이력
git log --follow -p -- path/to/file

# 변경된 파일 목록만
git diff --name-only

# 누가 수정했는지
git blame path/to/file

# 특정 커밋 내용 보기
git show <commit-hash>

# 삭제된 브랜치 복구
git reflog
git checkout -b recovered <commit-hash>
```

---

## 📋 충돌 해결

### 충돌 발생 시

```bash
# 1. 충돌 파일 확인
git status

# 2. 충돌 마커 확인
<<<<<<< HEAD
현재 브랜치 코드
=======
머지하려는 브랜치 코드
>>>>>>> feature/other

# 3. 수동으로 해결 후
git add <resolved-file>
git commit -m "Merge conflict resolved"

# 또는 머지 취소
git merge --abort
```

### 충돌 방지 팁

```
1. 자주 main과 동기화
   git pull origin main --rebase

2. 작은 단위로 자주 PR
   
3. 같은 파일 동시 수정 피하기
   
4. 커뮤니케이션 (같은 영역 작업 시 공유)
```

---

## 📋 Git 워크플로우 체크리스트

```
브랜치:
[ ] 의미 있는 브랜치명?
[ ] 최신 main에서 분기?

커밋:
[ ] 작은 단위 커밋?
[ ] 명확한 커밋 메시지?
[ ] 하나의 목적?

PR:
[ ] PR 설명 작성?
[ ] 테스트 통과?
[ ] 리뷰어 지정?

머지:
[ ] 충돌 해결?
[ ] CI 통과?
[ ] 브랜치 삭제?
```

---

## 🚫 Git 안티패턴

```
❌ main에 직접 푸시
❌ force push (공유 브랜치에)
❌ 너무 큰 커밋
❌ "WIP", "fix" 같은 불명확한 메시지
❌ 비밀정보 커밋 (API 키, 비밀번호)
❌ node_modules, .env 커밋
```
